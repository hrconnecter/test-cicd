# name: Central Workflow

# on:
#   workflow_dispatch:  # Manually trigger the workflow

# jobs:
#   run-selenium:
#     uses: ./.github/workflows/Selenium.yml  # Use the reusable Selenium workflow




name: Central Workflow

on:
  workflow_dispatch:

jobs:
  run-build:
    uses: ./.github/workflows/build.yaml

  run-sonarqube:
    needs: run-build
    uses: ./.github/workflows/SonarQube.yml
    with:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}  # This will pass SONAR_TOKEN as an environment variable
      SONAR_HOST_URL: http://13.202.182.26:9000  # Pass the URL directly as an environment variable

