# name: Central Workflow

# on:
#   workflow_dispatch:  # Manually trigger the workflow

# jobs:
#   run-selenium:
#     uses: ./.github/workflows/Selenium.yml  # Use the reusable Selenium workflow




name: Central Workflow

on:
  workflow_dispatch:

jobs:
  run-build:
    uses: ./.github/workflows/build.yaml

  run-sonarqube:
    needs: run-build
    uses: ./.github/workflows/SonarQube.yml
    with:
      SONAR_HOST_URL: http://13.202.182.26:9000
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  run-selenium:
    needs: run-sonarqube  # Wait for SonarQube to finish
    uses: ./.github/workflows/Selenium.yml
